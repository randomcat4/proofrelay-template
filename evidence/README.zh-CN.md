# 证据

[English](README.md)

把承重的可复现程序和紧凑输出放在这里，或放在所属尝试目录。每项计算主张都需要收据。使用 `python scripts/update_manifest.py` 把保全工件加入 `manifests/artifacts.json`。

大型生成文件通常应能由已提交源码再现。确需外部存储时，记录稳定位置、访问策略、字节数、SHA-256 和再生成说明。
